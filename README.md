The model's predictions are adjusted using numerical values, including those from the SHAP Waterfall Plot, and then integrated into the webpage frontend for presentation.

## Run Locally

**Prerequisites:**  Node.js

1. Install dependencies:
   `npm install`
2. Set the `GEMINI_API_KEY` in [.env.local](.env.local) to your Gemini API key
3. Run the app:
   `npm run dev`

**Step 1**
